# Não versiona a planilha de dados (fica só na máquina de quem gera o painel)
data/
*.xlsx
!assets/**

# docs/index.html É VERSIONADO DE PROPÓSITO: é o que o GitHub Pages publica.
# Sempre que rodar build_dashboard.py, o arquivo será sobrescrito — inclua a
# mudança no próximo commit para atualizar o link público.

__pycache__/
*.pyc

# backend/ (API do dashboard web — ver ARQUITETURA_DASHBOARD_WEB.md)
backend/.env
backend/.venv/
backend/*.db
